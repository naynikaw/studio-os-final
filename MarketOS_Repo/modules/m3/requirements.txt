# FastAPI and server dependencies
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.12

# Module M2 dependencies
pandas==2.2.3
praw==7.7.1
python-dateutil==2.9.0
packaging==24.2

# Module M3 dependencies
arxiv==2.3.0
orjson==3.10.7
# tavily-python==0.3.0  -- erroing out during build, check later
# requests==2.32.3

# Module M4 dependencies
openai==1.54.3
httpx==0.27.2  # Pin to version that supports 'proxies' parameter (required by openai)
# tiktoken is automatically installed as a dependency of openai (with wheels)

# Module M5 dependencies
# (uses json, textwrap - all standard)

# Module M6 dependencies
# (uses requests - covered above)
