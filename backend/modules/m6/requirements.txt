# FastAPI and server dependencies
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.12

# Module M2 dependencies
pandas==2.2.3
praw==7.7.1
python-dateutil==2.9.0
packaging==24.2

# Module M3 dependencies
arxiv==2.3.0
orjson==3.10.7
# requests==2.32.3  # NOTE: requests is pulled in transitively, keep commented unless you need a specific pin

# Module M4 dependencies
# (uses OpenAI Chat Completions API)
openai>=1.44.0

# Module M5 dependencies
# (uses google-generativeai, json, textwrap – all standard)

# Module M6 dependencies
# (uses openai + requests; requests covered above / via transitive deps)

# Shared LLM dependencies
google-generativeai==0.7.2
httpx==0.27.2

